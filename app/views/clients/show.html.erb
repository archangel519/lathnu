<div class="help-container">
  <p>Place the camera over the square icon on the page. Having trouble watching the video?</p>
  <button id="video-fullscreen" class="btn orange" type="button"><span>Full Screen Video</span></button>
</div>

<a-scene embedded arjs='sourceType: webcam; debugUIEnabled: false;'> 
	<a-assets>
		<video id="client-video" autoplay loop="true" src="<%= asset_path("videos/#{@client}.mp4") %>"></video>
	</a-assets>
	<a-marker preset='hiro'> 
		<a-video id="inputVideo" src="#<%= @client %>" rotation="90 180 0" position="0 0 0"></a-video> 
	</a-marker> 
	<a-camera></a-camera> 
</a-scene>